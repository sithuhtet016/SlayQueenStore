name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    name: Build, Typecheck, Lint & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'            # caches ~/.npm and node_modules for faster runs

      - name: Install dependencies
        run: npm ci

      - name: Run linter (if configured)
        run: npm run lint --if-present

      - name: Type-check (if configured)
        run: npm run typecheck --if-present

      - name: Build (if configured)
        run: npm run build --if-present

      - name: Run tests
        run: npm test

      - name: Upload test results (JUnit/coverage) - optional
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            coverage
            test-results.xml

# Optional: build and push Docker image on pushes to main
# Uncomment and configure if you want automatic image builds & push.
# Make sure to set appropriate secrets (DOCKERHUB_USERNAME/DOCKERHUB_TOKEN or use GHCR with GITHUB_TOKEN).
#
#  docker:
#    name: Build & Push Docker image
#    runs-on: ubuntu-latest
#    needs: build-and-test
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#    steps:
#      - uses: actions/checkout@v4
#      - name: Build image
#        run: docker build -t ghcr.io/${{ github.repository_owner }}/slayqueenstore:${{ github.sha }} .
#      - name: Log in to GitHub Container Registry
#        uses: docker/login-action@v2
#        with:
#          registry: ghcr.io
#          username: ${{ github.actor }}
#          password: ${{ secrets.GITHUB_TOKEN }}
#      - name: Push image
#        run: docker push ghcr.io/${{ github.repository_owner }}/slayqueenstore:${{ github.sha }}